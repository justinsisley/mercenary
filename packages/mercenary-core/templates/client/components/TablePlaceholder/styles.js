import jss from '../../utils/jss';

const styles = {
  placeholder: {
    height: 20,
    background: '#efefef',
    borderRadius: 3,
  },
};

export default jss.createStyleSheet(styles).attach().classes;
