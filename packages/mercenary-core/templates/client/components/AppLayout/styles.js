import { StyleSheet } from 'aphrodite-jss';

const styles = {
  content: {
    position: 'fixed',
    top: 0,
    paddingTop: '10px',
    paddingRight: '20px',
    paddingLeft: '240px',
    width: '100%',
    height: '100vh',
    overflow: 'scroll',
  },
};

export default StyleSheet.create(styles);
