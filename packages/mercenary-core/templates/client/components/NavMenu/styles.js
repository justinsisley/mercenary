import { StyleSheet } from 'aphrodite-jss';

const styles = {
  menu: {
    position: 'fixed',
    zIndex: 1,
    height: '100vh',
    borderRadius: 0,
    borderTop: 'none',
    borderBottom: 'none',
    boxShadow: 'none',
  },
};

export default StyleSheet.create(styles);
